var nam = Array();
  for(c=0; c<=4; c++){
      nam = parseFloat(prompt("Enter number"));
  }
    sum=0;
var nam =  [10, 20, 30, 40, 50] 
    for(var c=0; c<=4; c++){
     sum=sum+(nam[c]);
    }
document.write(sum);